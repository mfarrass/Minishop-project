module.exports = {
  // isi content disini isi file yg harus tailwind baca set default
  content: ["./src/**/*.{html,js}"],
  theme: {
    // container memberi setting agar berada ditengah dan memiliki padding
    container: {
      center: true,
      padding: '1,5rem'
    },
    extend: {},
  },
  plugins: [],
}
